import React from 'react';
import { StyledLoader } from './styled';

const Loader: React.FC = () => (
  <StyledLoader>
    <div>
      <div />
      <div />
      <div />
    </div>
  </StyledLoader>
);

export default Loader;
